// Manipular elemento HTML con JS

const heading = document.querySelector(".header p");
// heading.textContent = "SE CAMBIA EL HEADING"
console.log(heading.textContent);

const input = document.querySelector(".formulario #nombre");
input.value = "CAMBIO ACÁ";
console.log(input);